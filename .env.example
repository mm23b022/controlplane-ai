# Copy to .env and fill in. Without keys, ControlPlane runs on the mock provider.

# --- Providers (TODO[FILL]) -------------------------------------------------
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# --- Runtime ----------------------------------------------------------------
# mock | openai | anthropic
CP_DEFAULT_PROVIDER=mock
CP_AUDIT_DB=./controlplane_audit.db
CP_CACHE_TTL=300
CP_MAX_ATTEMPTS=2
